{% render 'social-icons',  icon_size: 'icon-small' %}
{% schema %}
{
  "name": "t:sections.all.settings.show_social.label",
  "class": "social-icons-wrapper",
  "presets": [
    {
      "name": "t:sections.all.settings.show_social.label",
    }
  ]
}
{% endschema %}
