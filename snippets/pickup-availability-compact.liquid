<pickup-availability-compact class="no-js-hidden product-card-pickup-availability text-xsmall" data-base-url="{{ routes.root_url }}" data-variant-id="{{ product.selected_or_first_available_variant.id }}" data-has-only-default-variant="{{ product.has_only_default_variant }}" data-id="{{ section.id }}">
  
</pickup-availability-compact>